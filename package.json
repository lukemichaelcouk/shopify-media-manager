{
  "name": "shopify-media-manager",
  "version": "1.0.0",
  "description": "A static web application for managing Shopify store media files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server\" \"npm run functions\"",
    "server": "node server.js",
    "functions": "netlify dev",
    "build": "echo 'No build step required for static site'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "shopify",
    "media",
    "management",
    "oauth",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "image-size": "^2.0.2",
    "multer": "^2.0.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
